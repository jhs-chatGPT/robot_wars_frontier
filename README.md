# ROBOT WARS FRONTIER — React v0.9.14

## 9271 확정안 반영
- 기준 소스: v0.9.12 통합본 + v0.9.13 홈 구현.
- 기준 이미지: `docs/reference-9271.jpg`.
- 1920×1080 기준 상단 세 패널 / 중단 여섯 바로가기 / 하단 세 패널의 높이·폭·여백 보정.
- 금색 작전 준비 버튼, 파란 금속 메뉴/바로가기 버튼을 독립 PNG로 제작해 적용.
- TOP HUD·좌측 외곽·E.F. FORCE 패널에 래스터 금속 프레임 적용(홈 화면 범위).
- 참고 이미지와 같은 계열의 궤도 격납고 배경을 제작해 우주 작전·이벤트 미리보기·본문 배경에 적용.
- 이름·능력치·EXP·메뉴 버튼은 React 요소로 구현. 전체 스크린샷을 화면에 깔아 클릭만 얹는 방식이 아님.
- 기존 지휘관·기체 자산과 실제 저장 데이터는 유지. 따라서 캐릭터 일러스트와 수치는 시안의 예시와 다를 수 있음.
- 이벤트 운영 데이터는 아직 없으므로 준비 중/기간 미정 표시 유지.
- 다른 메뉴 화면의 본문 및 전투 규칙은 변경하지 않음. 일일 임무는 v0.9.13 구현 유지.

## 전달·검증 상태
**구현 소스 전달본이며 실행 화면 검증 완료본이 아닙니다.**
새 이미지 3개 무결성, 새 정적 경로 7개, CSS 블록 균형을 확인했습니다.
이전 단계의 일일 임무 4개 테스트 통과 결과는 그대로 유효하며 이번 버전에서 관련 로직은 변경하지 않았습니다.
전체 TypeScript/Vite 빌드와 브라우저 시각 비교는 아직 완료하지 못했습니다. 기존 환경의 npm 접근 차단 및 브라우저 부재로 검증이 제한되어 있습니다.

## 설치
- `patch` ZIP은 원본 v0.9.12 대비 누적 수정본입니다. v0.9.12 또는 이전에 전달한 v0.9.13 소스 루트에 덮어씁니다.
- `full` ZIP은 모든 소스를 포함합니다. 새 폴더에 풀어서 사용할 수 있습니다.
- 기존 게임·저장 데이터는 백업한 뒤 적용하십시오.
- `npm install` → `npm run build` → `npm run dev`.
- Node 22.18 이상에서 `npm run test:daily`.
- `netlify.toml` 유지. 이번 작업에서 배포하지 않았습니다.
- 새 UI 경로: `public/ui/`. 기존 프로젝트의 `public/assets/` 경로도 유지합니다.

## 다음 실행 확인
1. 1920×1080, 브라우저 확대 100%에서 `docs/reference-9271.jpg`와 홈 배치 비교.
2. 1536×864, 1280×720, 768×1024, 390×844에서 글자·버튼 잘림 확인.
3. 여섯 바로가기·주력 기체 상세·작전 준비·공지 닫기·일일 임무 보상 확인.
4. 시각 차이와 빌드 오류를 해결한 뒤 배포.

---
## v0.9.13 작업 기록

# ROBOT WARS FRONTIER — React v0.9.13

## 이번 전달본
기준: `robot_wars_frontier_react_v0.9.12_stage1_shell_rebuild_complete.zip`
시각 기준: 확정된 `9213.png`. 홈 화면의 상단 3패널 / 바로가기 6개 / 하단 3패널을 구현했습니다.

- 지휘관·기체·EXP·자원은 실제 저장 데이터로 표시됩니다. 시안의 한도윤/Lv.24/화이트아크 수치를 강제로 넣지 않습니다.
- 주력 기체 상세는 해당 기체를 선택한 개조 화면, 작전 준비는 기존 작전 목록으로 연결됩니다.
- 기체 이미지는 기존 프로젝트 자산을 사용하며 전체 비율을 유지합니다.
- 공지는 이 버전의 기능 안내입니다. 별도 온라인 공지 서버는 없습니다.
- 이벤트는 운영 데이터가 없어 준비 중/기간 미정으로 명시합니다. 가짜 진행 기간이나 특별 보상은 표시하지 않습니다.
- 일일 임무: 한국 시간 자정 초기화, 적용 후 실제 행동부터 집계, 계정 공통 저장, 완료 보상 중복 수령 방지.
- 작전 완료는 승리 시 집계하고, 격파 수는 기존 전투 결과가 확정될 때 반영합니다.
- 좁은 화면에서는 본문을 세로로 재배치합니다. 1920×1080에서 전체 3단 배치하도록 작성했습니다.
- 새 래스터 프레임은 `public/ui/console-panel.png`, 새 CSS는 `src/command-home.css`입니다.

## 검증 상태 — 필독
- 일일 임무 로직 테스트 4개 통과, 스토어 TypeScript 문법 검사 통과, 새 정적 자산 경로 검사 통과.
- 외부 npm 다운로드가 HTTP 403으로 차단되어 **TypeScript 전체 타입 검사와 프로덕션 빌드는 미완료**입니다.
- 브라우저 검증 도구가 없어 **1920×1080 화면 비교, 반응형 화면, 실제 버튼 조작 검증은 미완료**입니다.
- 따라서 이 ZIP은 검증 제한이 있는 구현 소스입니다. 배포 완료본 또는 시안과 픽셀 단위로 일치함을 보증하는 결과물이 아닙니다.
- 기존 `FINAL_VALIDATION.md`, `SHELL_VALIDATION.md` 등은 이전 버전 기록입니다. 이번 변경의 상태는 `design-qa.md`를 따릅니다.

## 적용
1. 기존 프로젝트와 설정 메뉴의 저장 데이터를 백업합니다.
2. 패치 ZIP은 v0.9.12 프로젝트 루트에 덮어씁니다. 통합 ZIP은 새 폴더에 풉니다.
3. Node 22.12 이상에서 `npm install`, `npm run build`를 실행합니다.
4. `npm run dev`로 홈/기체 상세/작전 준비/바로가기/공지/임무 수령을 확인합니다.
5. 일일 임무 테스트: Node 22.18 이상에서 `npm run test:daily`.
6. Netlify 설정은 기존 `netlify.toml`을 유지합니다. 이번 작업에서 서버 배포는 수행하지 않았습니다.

---
## 이전 버전 설명 (이력)

# ROBOT WARS FRONTIER — React v0.9.10

기존 **v0.8 P15.6.23 안정화판**을 기준으로 React로 이전한 ROBOT WARS FRONTIER 프로젝트입니다. v0.9.10에서는 v0.9.9 HUD 적용 후 실제 1536×1024 화면에서 확인된 축소감·과도한 빈 공간·화면 간 밀도 불일치를 바로잡는 UI 안정화 핫픽스를 적용했습니다.

## v0.9.10 핵심 변경
- TOP HUD 신형 디자인 유지 + 높이/로고/자원 패널 비율 확대
- 좌측 메뉴 폭/행 높이/아이콘/글자 크기를 실사용 해상도에 맞게 재조정
- 홈 화면의 과도한 빈 공간을 줄이고 지휘관/주력기체/다음작전 중심 밀도로 재배치
- 파일럿 관리의 목록/정보/훈련 가독성 및 카드 밀도 확대
- 파일럿 능력치에서 `지휘` 삭제
- 현재 능력치: `격투 / 사격 / 반응 / 조종 / 방어 / 기량`
- `특수스킬` 표현을 `특수능력`으로 정리
- 특수능력은 `습득 / 강화` 모달 방식으로 변경
- 특수능력 Lv 데이터를 실제 전투 보정에서 읽도록 개선
- 기존 저장 데이터의 `command` 항목은 로드 시 자동 제거/정규화


### 전체 메뉴 UI 안정화
- 파일럿 영입 / 기체 목록 / 기체 개발 / 강화파츠 / 상점 / 도감 / PvP / 대회 / 관리자 / 설정 / 전투 / 작전 출격의 글자와 카드 밀도를 페이지별로 보정
- 기존 화면의 내부 기능 레이아웃은 유지하고 TOP/사이드바/패널 톤만 같은 FRONTIER HUD 계열로 통일
- 신규 안정화 CSS는 `data-page="..."` 범위로 한정해 다른 메뉴의 div에 영향을 주지 않도록 구성

## 기술 구성
- React 19
- Vite 7
- TypeScript
- Zustand + localStorage 자동 저장
- Netlify 정적 배포

## 주요 화면 / 기능
- 타이틀 / 이어하기
- 파일럿 선택 → 리얼/슈퍼 → 초기 기체 선택 → 홈
- 홈 / 다음 작전
- 파일럿 관리 / PP 능력치·지형·특수능력 육성
- 파일럿 영입 / 주력 파일럿 교체
- 기체 개발 / 10단 개조
- 기체 목록
- 강화파츠 독립 화면 / 슬롯 장착·해제
- 상점 / 강화파츠 구매
- 도감 / 기체·무장 데이터
- 10개 시나리오 / 다중 웨이브 / 텍스트 전투
- 정신기 / 거리 이동 / 방어 / 자동 행동
- PvP 자동 텍스트 교전
- AI 대회 / 직접 개최 / 수비 편성 / 상품 / 결과 로그
- 관리자 데이터 추가
- JSON 저장 백업 / 복원
- 설정 / 타이틀 복귀

## 저장 데이터 호환
React판은 `rwf-react-v0.9-save` 키로 자동 저장됩니다.
기존 P15.6.23 저장 데이터가 있고 React 저장 데이터가 없다면 다음 레거시 키를 자동 감지합니다.

- `robot_wars_proto_v8_ui_match`
- `robot_wars_proto_v8`
- `robot_wars_proto_v2`
- `robot_wars_proto_v1`

v0.9.9부터 예전 저장 데이터에 `stats.command`가 남아 있어도 로드 시 현재 6능력 구조로 자동 정규화합니다.

## 실행
```bash
npm install
npm run dev
```

## 프로덕션 빌드
```bash
npm run build
```

빌드 결과는 `dist/`에 생성됩니다.

## Netlify
`netlify.toml` 설정:
- Build command: `npm run build`
- Publish directory: `dist`
- SPA fallback: `/* -> /index.html`

GitHub 저장소 루트에 프로젝트 파일을 그대로 업로드하면 Netlify Git 배포에 사용할 수 있습니다.

## 검증
현재 런타임에는 `node_modules`가 없어 실제 Vite production build는 실행하지 못했습니다. 대신 전체 TypeScript 소스 구문 검사, 임시 React/Zustand 타입 선언을 이용한 전체 `src` 의미 검사, CSS 괄호 검사, `/assets` 참조 누락 검사를 수행했고 v0.9.10 변경으로 인한 코드 오류는 발견되지 않았습니다.

자세한 내용은 `FINAL_VALIDATION.md`를 참고하세요.
